/build/
/dist/
/nbproject/private/
JSpeccy.xml
/target/
.idea/
*.log
.settings/*
*.iml
*.dot
*.class
*.jfr
*/target/**
**/target/**
dependency-reduced-pom.xml
.profileconfig.json

# The reference emulator's C source and library, kept alongside to check against. GPL, and this
# repo is Apache: they must never be committed here.
fuse-emulator-fuse/
libspectrum/