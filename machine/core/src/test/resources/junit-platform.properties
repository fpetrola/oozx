# A hung test must fail, not stall the run: the emulator's command bridge spins on a queue that
# a stopped machine never feeds, which cannot be interrupted from the test's own thread.
junit.jupiter.execution.timeout.default = 60s
junit.jupiter.execution.timeout.thread.mode.default = SEPARATE_THREAD
